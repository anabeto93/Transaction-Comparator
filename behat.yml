default:
#    suites:
#        authentication:
#            contexts: [ FeatureContext, AuthenticationContext ]
#            filters: { tags: '@authentication' }

    extensions:
        Laracasts\Behat:
            env_path: .env.behat
        Behat\MinkExtension:
            default_session: laravel
            laravel: ~
